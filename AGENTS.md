# AGENTS.md

## 项目定位

FastPartitionVTM 用于构建一个面向 **VVC 标准参考软件 VTM 24.0** 的、基于神经网络方法的快速划分方案。

本文件作为项目根目录下的统一协作与目录使用规范，约定各子目录职责、外部数据依赖、后续模型补充方式，以及开发与实验过程中的边界，避免将开发仓库、标准参考模型、本地基线副本、训练数据和网络模型资源混用。

## 顶层目录职责

### `VVCSoftware_VTM/`
- 该目录是当前项目中用于集成和开发快速划分方案的主工作目录。
- 该目录当前为一个与远程关联的 Git 仓库，可进行后续代码修改、实验性集成、版本管理与提交。
- 与 VTM 编码器/解码器实现、快速划分逻辑接入、工程构建配置、实验性编码流程调整相关的改动，应优先放在该目录中完成。

### `ref_model/`
- 该目录用于放置 **标准 VTM 24.0 执行模型** 的最小化本地基线副本。
- 该目录默认不复制 VTM 源码，仅保留运行与对比所需内容。
- 当前标准组织形式为：
  - `build/`：存放最小可运行的标准、未改动 VTM 24.0 可执行文件集合；
  - `cfg/`：存放编码与测试所需配置；
  - `script/`：存放运行脚本与后续性能测试脚本。
- 该目录的主要职责是：
  - 训练数据生成；
  - 标准基线性能对比；
  - 参考验证与复现实验。
- 默认情况下，`ref_model/` **不作为远程开发仓库使用**，也不作为新方法长期开发的主承载目录。
- 不应直接在 `ref_model/` 中承载神经网络快速划分方法的主要开发工作，除非后续有明确的新规范说明。

### `network/`
- 该目录用于存放网络模型相关内容。
- 目前和后续补充的内容可包括但不限于：
  - 模型结构定义；
  - 训练脚本；
  - 训练数据或训练数据索引；
  - 推理脚本与模型导出脚本；
  - 实验配置；
  - 结果摘要、实验记录与模型版本说明。
- 后续新增到 `network/` 下的模型目录，应至少附带最小说明，明确其：
  - 用途；
  - 输入与输出；
  - 与训练数据、推理流程或 VTM 集成流程之间的依赖关系。
- 当前 `Classifier_I` 相关约定：
  - 训练策略文档位于 `network/train_classifier.md`；
  - VTM 划分合法性与 mask 备注位于 `network/vtm_split_mask_notes.md`；
  - 第一阶段逻辑预训练数据由 `network/src/partition.py` 生成到 `data/dataset/Classifier_I/pretrain_logical/`；
  - 该数据目录按 grid ROI 尺寸分层，每个子目录仅保留 `gridmap.npy` 与 `label.npy`；
  - `Classifier_I` 类别顺序固定为 `[NO_SPLIT, QT, BTH, BTV, TTH, TTV]`；
  - `Classifier_I.forward()` 默认返回 masked logits，训练时使用 logits + `CrossEntropyLoss`，推理时再 softmax。

## 外部数据依赖

- 当前训练与测试所用视频序列位于外部目录：`~/qiujp/HEVC_CTC_YUV`。
- 该路径属于仓库外部依赖，不应将原始视频序列直接纳入本仓库版本管理。
- 涉及数据读取的代码、脚本或配置，应优先使用**可配置路径**引用该目录，而不是将路径硬编码为不可替换形式。
- 若后续需要共享实验配置，应优先共享数据索引、列表文件或路径配置模板，而不是复制原始序列进入仓库。

## 工作环境约定

- 整体工作环境应位于通过 Conda 创建的 **`FastPartitionVTM`** 工作环境下。
- 与本项目相关的编译、脚本运行、训练、推理和性能测试操作，默认均应在激活该环境后进行。
- 后续若补充 Python 依赖、测试依赖或工具链说明，应优先以 `FastPartitionVTM` Conda 环境为统一承载环境，避免不同子模块使用彼此割裂的运行环境。
- 若存在系统级编译依赖或外部二进制依赖，也应在文档或脚本中明确其与 `FastPartitionVTM` 环境的关系。
- 当前项目已额外安装系统级 `cmake` 用于标准 VTM 24.0 的编译；除该类系统工具外，其余项目相关操作仍应优先在 `FastPartitionVTM` Conda 环境中完成。

## 协作边界与推荐目录观

### 推荐职责分工
- 与 VTM 代码实现相关的改动，优先放在 `VVCSoftware_VTM/`。
- 与标准基线复现、原始行为对照、训练标签或对比数据生成相关的执行，优先基于 `ref_model/` 中的 `build/`、`cfg/` 和 `script/` 组织完成。
- 与训练、模型管理、样本生成脚本、网络推理、模型导出和实验记录相关的内容，统一放在 `network/`。

### 组织原则
- 将“标准基线”和“实验性方法开发”明确分离。
- 将“网络模型资产”和“VTM 工程代码”明确分离。
- 将“仓库长期保留内容”和“临时实验产物”明确分离。

## 禁止事项与注意事项

- 不直接修改 `ref_model/` 中的标准基线模型来承载新方法开发。
- 不将大体积训练数据、大体积模型权重、编码输出结果或临时日志直接混入主代码目录。
- 不将临时实验产物与长期保留的模型、脚本和配置混放。
- 后续新增 `network/` 子目录时，应补充最小说明，避免目录用途不清或输入输出关系不明。

## 工作流占位

以下章节为当前版本的占位说明，后续可在项目实际流程稳定后补充细化。

### 1. 数据生成
- 训练数据生成流程将基于标准 VTM 基线与实验模型共同参与。
- `ref_model/` 侧重提供标准参考行为与基线输出。
- `VVCSoftware_VTM/` 中的实验实现可用于生成与快速划分策略相关的附加数据或对比结果。
- 具体数据格式、标签定义、生成脚本位置与命名规则后续单独补充。

### 2. 模型训练
- 模型训练相关脚本、数据索引、配置文件、权重文件与训练记录，未来统一放在 `network/` 下管理。
- 当前不预设具体训练框架，不提前限定为 PyTorch、ONNX、TensorRT 或其他实现形式。
- 待模型目录补充完成后，再进一步统一训练入口、依赖环境和结果保存规范。
- 当前 `network/src/train.py` 通过 `--task` 选择训练功能：
  - `classifier_pretrain_logical`：训练 `Classifier_I` 第一阶段逻辑预训练模型；
  - `swin_luma`：训练亮度 Swin-UNet gridmap 预测模型。
- 训练脚本中的 `--outDir` 表示 `network/output/` 与 `network/checkpoints/` 下的实验子目录名，不应传入临时绝对路径作为长期实验默认值。
- 训练日志、`loss.txt`、`train.log` 应保存到 `network/output/<outDir>/<jobID>/`；模型权重应保存到 `network/checkpoints/<outDir>/<jobID>/`。
- `network/src/train.py` 会自动 tee 终端输出到 `network/output/<outDir>/<jobID>/<logFile>`，后台运行时无需额外创建日志目录。

### 3. 性能评测
- 后续评测应至少关注以下维度：
  - 编码复杂度变化；
  - BD-rate 或质量损失；
  - 划分决策一致性或近似程度；
  - 与标准基线之间的性能对比。
- 具体评测脚本、统计方式、结果汇总格式后续补充。

### 4. 结果管理
- 后续应在 `network/` 或约定的实验结果目录下统一管理：
  - 实验记录；
  - 日志；
  - 模型版本说明；
  - 关键指标摘要。
- 推荐后续建立统一命名规范，以区分训练结果、推理结果、对比结果和临时调试输出。

## 当前默认假设

- `ref_model/` 当前采用最小化组织方式，目标是仅保留标准、未改动 VTM 24.0 的最小可运行可执行文件、运行所需配置与脚本，而不复制整份源码。
- 你后续会将模型相关目录补充到 `network/` 中，因此本版规范先给出组织原则与工作流占位，不预设具体模型结构与框架细节。
- 当前不在本文件中写入具体训练命令、构建命令、评测脚本路径或命名规则细则，除非后续仓库中已经形成稳定实现并需要固化。
- 当前 `ref_model/` 已补充 `cfg/`、`script/` 目录，并基于未改动的 `VVCSoftware_VTM` 提取出一份最小可运行的标准 VTM 24.0 `build/` 内容。

## 使用建议

- 后续任何自动化代理、协作者或脚本在修改本仓库前，应优先遵循本文件中的目录职责、环境要求与边界约束。
- 当项目结构发生明显扩展时，应同步更新本文件，确保目录职责、数据路径依赖和实验流程说明保持一致。
